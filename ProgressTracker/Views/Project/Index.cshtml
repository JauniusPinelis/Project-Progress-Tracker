@model IEnumerable<ProgressTracker.Models.ProjectModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Add new project", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TimeSpentInMinutes)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StartTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TimeSpentInMinutes)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", "ProjectTime", new { id=item.Id }, null) |
            @Html.ActionLink("Details", "Details", "ProjectTime", new { id=item.Id }, null) |
            @Html.ActionLink("Delete", "Delete", "ProjectTime", new { id=item.Id }, null)
        </td>
    </tr>
}

</table>
